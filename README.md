Basic .vim folder for linux:

        $ git clone git://github.com/rball325/dotvim
        $ cd dotvim
        $ git submodule init
        $ git submodule update
        $ mv dotvim .vim
        $ ln -s .vim/.vimrc .vimrc
